1.ps -ef -aux  //查看进程

2.kill
kill -s 9 PID //最高等级
pgrep firefox | xargs kill -s 9//前面输出的结果作为进程杀死的对象

3.grep
//查找带有关键字的部分,通常与管道结合
例如 grep firefox
grep -v firefox   //反向选择
grep -c firefox   //计算查找次数

4.pgrep
专门用户查找进程号
例如 pgrep nc     pgrep firefox

5.cut
对于显示进行截取
cut -c 9-15       //要9-15行
cut -d 'k' -f 5   //用d分割第5列

6.pkill
pkill -9 进程名字 //以等级9杀死进程== pgrep+kill
例如 pkill -9 firefox   pkill -9 nc

7.>和|
>是重定向  重定向到文件
stdout >  文件   stdout输出到文件(覆盖)
stdout >> 文件   stdout输出到文件(追加)
stdout <  文件   文件输入到stdin

|是管道    管道后端接受命令
//前面和后面的-符号代表管道的两端传输的东西，有时需要用例如tar -cvf - /file | tar xvf -


8.tr
//进行删除或者替换
-d 删除
-c 替换


9.awk
awk '{print $i}' //$0表示所有域
awk -F ':' '{print $1}' //-F指定分割方式
awk -F ':' '{print $1"\t"$7}' //输出制表符
awk -F ':' 'BEGIN {print "name,shell"} {print $1"\t"$7} END {print "blue,/bin/nosh"}' //开头和末尾添加
awk -F: '/root/{print $1}' /etc/passwd  //显示/etc/passwd中所有有root的行
awk '{count++;print $1} END {print "count is:",count}' /etc/passwd  //记录/etc/passwd行的个数并打印
awk 'BEGIN {size=0} {size=size+$5} END {print "size is:",size}'   //统计文件夹大小
awk 支持if while do/while for continue break 等语句,和c一样

10.last
显示用户最近的登陆信息

11.sort
sort apple.txt
-u去掉重复行
-r 降序
-o 输出到指定文件
-n 按照数值排序(默认字典顺序)
-t 指定分隔符 -k 指定分割后到列数  然后进行排序
sort -t ' ' -k 1.2,1.3 //以' '分割并且，以第一块的第2-3部分进行排序依据
-f 忽略大小写
-M 按月份排序

12.dpkg
dpkg -L command 列出这个命令下所有的目录

13.gzip
gzip -d filename.gz解压gz文件

14.wc
-c统计字节数
-w统计字数
-l统计行数

15.netstat
netstat -pantu
netstat -n 查看连接状态

16.rdesktop
远程连接windows桌面

17.top
监控cpu和内存
同一网段不明显

18.iptables
-I OUTPUT 链前插入
-A OUTPUT 链尾插入
-i eth0 网卡流入
-o eth0 网卡流出
-nvL 显示网络,详细,列出
iptables -t filter -F INPUT 删除INPUT 
iptables -I INPUT -s 192.168.0.108  -p tcp -m tcp --dport 22:25 -j REJECT
//默认filter -m, tcp 指定tcp的扩展模块是--dport, -p是tcp, -m默认是tcp, dport对应sport 
-m multiport --dport 21,24,26,33:44指定离散端口

19.chvt
切换终端界面
